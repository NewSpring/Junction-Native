// @flow
import Style from "further";

export default Style.of({
  visibility: "visible",
  height: "inherit",
  maxHeight: "inherit",
  margin: "inherit",
  paddingTop: "inherit",
  paddingBottom: "inherit",
  opacity: 1,
});
